<UserControl x:Class="WorkLogWpf.Views.Controls.WeekCalendarVertical.CalendarBlock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="30"
             MinHeight="30"
             Cursor="SizeAll">
    <Border x:Name="BlockBorder"
            Background="#69B5FF"
            BorderBrush="#1460AA"
            BorderThickness="1"
            CornerRadius="4"
            SnapsToDevicePixels="True">
        <Grid>
            <!-- Resize úchyty -->
            <Thumb x:Name="TopThumb" Height="5" VerticalAlignment="Top" Cursor="SizeNS">
                <Thumb.Template>
                    <ControlTemplate TargetType="Thumb">
                        <Border CornerRadius="4" Background="DodgerBlue">
                        </Border>
                    </ControlTemplate>
                </Thumb.Template>
            </Thumb>

            <Thumb x:Name="BottomThumb" Height="5" VerticalAlignment="Bottom" Cursor="SizeNS">
                <Thumb.Template>
                    <ControlTemplate TargetType="Thumb">
                        <Border CornerRadius="4" Background="DodgerBlue">
                        </Border>
                    </ControlTemplate>
                </Thumb.Template>
            </Thumb>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Image Width="16" Height="16"
                       Margin="4,8,0,0"
                       x:Name="BlockImageInfo"
                       Grid.Row="0"
                       Grid.Column="0" Source="/Views/Controls/Assets/tools.png"/>
                <TextBlock x:Name="BlockTitle" Text="Titulek" Foreground="Black" HorizontalAlignment="Left" Margin="4"
                           Grid.Row="0"
                           Grid.Column="1"/>
                <TextBlock x:Name="BlockSubtitle" Text="Podtitulek" Foreground="Black" HorizontalAlignment="Left" Margin="4"
                           Grid.Row="1"
                           Grid.Column="1"/>
            </Grid>

        </Grid>
    </Border>
</UserControl>
